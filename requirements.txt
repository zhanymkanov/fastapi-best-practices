# ──────────────────────────────────────────────────────────────────────────────
# QMI宠物生态智能管理平台 — Python 依赖清单
# 更新原因：新增 A2A 多 Agent 编排、RAG 知识库检索、MCP 工具层
# ──────────────────────────────────────────────────────────────────────────────

# ── Web 框架 ──────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # 文件上传

# ── 数据验证 & 配置 ────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic[email]>=2.7.0
pydantic-settings>=2.5.0

# ── 数据库 ────────────────────────────────────────────────────────────────────
motor>=3.6.0                  # MongoDB 异步驱动
beanie>=1.27.0,<2.0.0         # Beanie ODM（基于 Motor）
redis>=5.0.0                  # Redis 异步客户端

# ── 向量数据库（RAG 核心） ────────────────────────────────────────────────────
pymilvus>=2.4.0               # Milvus 客户端（向量检索）

# ── 嵌入模型（RAG 向量化） ────────────────────────────────────────────────────
# 本地模式：使用 sentence-transformers + bge-m3 模型
sentence-transformers>=3.0.0  # 本地 embedding / reranker

# ── 认证 & 安全 ───────────────────────────────────────────────────────────────
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# ── HTTP 客户端（LLM API / IoT API 调用） ───────────────────────────────────
httpx>=0.27.0

# ── 任务队列 & 调度 ────────────────────────────────────────────────────────────
celery[redis]>=5.4.0          # Celery 任务队列（OTA 升级、消息推送等）

# ── 监控 & 日志 ───────────────────────────────────────────────────────────────
sentry-sdk>=2.0.0             # 错误追踪（生产环境）

# ── 配置文件解析 ──────────────────────────────────────────────────────────────
tomli>=2.0.0; python_version < "3.11"   # cyberlife.toml 配置解析

# ── Python 3.9 兼容 ──────────────────────────────────────────────────────────
eval_type_backport>=0.4.0    # Python <3.10 支持 X | Y 类型联合语法

# ── 可选：PDF 解析（RAG 文档入库） ───────────────────────────────────────────
# pymupdf>=1.24.0             # PDF 解析（如需支持 PDF 知识库入库，取消注释）

# ── Dev & Test ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.9.0
